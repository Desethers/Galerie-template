{
  "name": "almine-rech-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "http-server . -p ${PORT:-3006} -c-1",
    "start": "http-server . -p ${PORT:-3006} -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
